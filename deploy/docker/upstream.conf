server {
    listen 8090;
    server_name _;

    location / {
        default_type application/json;
        return 200 '{"ok":true,"upstream":"demo","path":"$uri","args":"$args","note":"protected content served by origin"}';
    }
}
